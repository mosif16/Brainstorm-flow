# Backend environment configuration
# Replace placeholder values before running the server.
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=models/gemini-1.5-pro-latest
PORT=4000
DEFAULT_N=6
DEFAULT_K=3
# Optional: point to a custom runs directory
RUNS_DIR=../runs
