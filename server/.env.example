# Backend environment configuration
# Replace placeholder values before running the server.
GEMINI_API_KEY=your-gemini-api-key
# Optional: override the Gemini model used by the Google GenAI SDK
GEMINI_MODEL=models/gemini-2.5-flash-lite
DEFAULT_N=6
DEFAULT_K=3
# Optional: point to a custom runs directory
RUNS_DIR=../runs
